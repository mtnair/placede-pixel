import React from 'react';
import Col from 'react-bootstrap/Col';

import './Metadata.css';

const Metadata: React.FC = () => {
  return (
    <Col md='2' className='metadata'>
      <span>Metadata</span>
    </Col>
  );
};

export default Metadata;
